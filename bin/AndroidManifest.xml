<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="apps.are.whosaidthat"
    android:versionCode="3"
    android:versionName="1.2" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="apps.are.whosaidthat.MainActivity"
            android:configChanges="orientation"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="apps.are.whosaidthat.MainGame"
            android:label="@string/title_activity_main_game" />
        <activity
            android:name="apps.are.whosaidthat.Variable"
            android:label="@string/title_activity_variable" >
        </activity>
        <activity
            android:name="apps.are.whosaidthat.TestShuffle"
            android:label="@string/title_activity_test_shuffle" >
        </activity>
    </application>

</manifest>
